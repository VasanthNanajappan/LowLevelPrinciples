The Open/Closed Principle (OCP) states that software entities like classes, modules, and functions
should be open for extension but closed for modification.

This means you should be able to add new functionality to an existing class or module without changing its existing code.